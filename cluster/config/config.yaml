apiVersion: v1
kind: ConfigMap
metadata:
  name: kube-dns
  namespace: kube-system
data:
  upstreamNameservers: |
    ["8.8.8.8", "192.168.1.1", "10.96.0.10", "192.168.1.200"]
---
      # [plugins."io.containerd.grpc.v1.cri".registry.configs]
      #       [plugins."io.containerd.grpc.v1.cri".registry.configs."registry.REPLACE_WITH_YOUR_DOMAIN:5000".tls]
      #          insecure_skip_verify = true

      # [plugins."io.containerd.grpc.v1.cri".registry.headers]

      # [plugins."io.containerd.grpc.v1.cri".registry.mirrors]
      #       [plugins."io.containerd.grpc.v1.cri".registry.mirrors."registry.REPLACE_WITH_YOUR_DOMAIN:5000"]
      #           endpoint = ["http://registry.REPLACE_WITH_YOUR_DOMAIN:5000"]
