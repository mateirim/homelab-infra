apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: syslog-config-pvc
  namespace: loki
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: nfs
  resources:
    requests:
      storage: 1Gi
